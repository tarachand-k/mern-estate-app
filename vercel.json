{
  "buildCommand": "cd client && npm install && npm run build && cd .. && cd server && npm install && prisma generate && node app.js",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/server/app.js"
    }
  ]
}
